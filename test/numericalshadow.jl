using FactCheck
using NumericalShadow

facts("numerical_shadow") do
  context("serial") do
  end

  context("parallel") do
  end
end

facts("real numerical shadow") do
  context("serial") do
  end

  context("parallel") do
  end
end
